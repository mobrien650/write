import React, { Component } from 'react'
import styled from 'styled-components'

const Wrapper = styled.div`
    display: flex;
    justify-content: flex-end;
`

export default (props) => {
    return <Wrapper>Copyright 2020. All rights resevered</Wrapper>
}